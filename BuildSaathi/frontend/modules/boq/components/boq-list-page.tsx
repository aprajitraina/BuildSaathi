"use client";

import { EstimatesLandingPage } from "./estimates-landing-page";

/** Estimates home — hero, type cards, upload panel, recent list. */
export function BOQListPage() {
  return <EstimatesLandingPage />;
}
